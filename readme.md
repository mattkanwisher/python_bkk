# Intro to block chain for Python Bangk Meetup

## Setup

```
pip install web3

npm install ethereumjs-testrpc web3
npm install ethereum/web3.js --save
npm install solc --save
npm install sleep --save
```